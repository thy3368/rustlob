# Reth Node Configuration Example
# Copy this file to .env and customize as needed

# Network Selection
# Options: mainnet, sepolia, holesky, goerli
# For testing, use sepolia (recommended for development)
CHAIN=sepolia

# Logging Configuration
# Options: error, warn, info, debug, trace
# Use 'info' for normal operation, 'debug' for troubleshooting
RUST_LOG=info

# Full Node Setup (Optional)
# Uncomment to run both execution (Reth) and consensus (Lighthouse) clients
# Then use: docker-compose --profile full-node up -d
# COMPOSE_PROFILES=full-node

# Advanced Configuration (Optional)
# Uncomment and customize as needed

# Maximum number of peers
# MAX_PEERS=50

# Enable pruning (saves disk space)
# PRUNING_MODE=full

# Database cache size (MB)
# DB_CACHE_SIZE=2048
