# Activation Gas Fee

New HyperCore accounts require 1 quote token (USDC, USDT, or USDH) as a one-time fee for the first transaction sent to that account, regardless of asset type.

Unactivated accounts can't send CoreWriter actions. Deployers can bypass this by manually sending an activation transaction to the EVM contract address on HyperCore.
