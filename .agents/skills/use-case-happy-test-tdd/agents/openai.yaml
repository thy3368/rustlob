interface:
  display_name: "Use Case Happy Test TDD"
  short_description: "Complete happy-path business matrices and drive core business fixes from missing spec tests"
  default_prompt: "Use $use-case-happy-test-tdd to review current RustLOB CommandUseCase4 happy coverage from real code, reconstruct the full happy-path business matrix, emit the fixed five-part scorecard, choose one highest-value missing matrix cell, write the failing happy spec test first, assert changes semantics before projection, and repair only the core pair-first changes semantics or replayable-event projection needed to close that exact gap without accepting duplicate after snapshots."
